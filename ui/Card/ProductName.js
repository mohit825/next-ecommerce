import { HeadingContainer } from "../../styles/styled-component/ProductPageContainerStyle";
const ProductName = ({ children }) => {
  return <HeadingContainer>{children}</HeadingContainer>;
};

export default ProductName;
